# Optional: pre-seed GitHub config server-side (not exposed to client)
# If set, the API route will use these as fallback when no PAT is provided in the request header.
# Leave blank to require per-user PAT entry in the UI.
GITHUB_PAT=
GITHUB_OWNER=
GITHUB_REPO=
