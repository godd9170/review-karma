# Server-side only (never exposed to browser) — used by the API route
GITHUB_PAT=

# Exposed to the client so the app knows to auto-connect on load.
# Set all three to skip the "Connect GitHub" UI entirely.
NEXT_PUBLIC_GITHUB_OWNER=
NEXT_PUBLIC_GITHUB_REPO=
